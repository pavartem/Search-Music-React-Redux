import {SEARCH_SUCCESS} from 'actionTypes'

const initialState = [];


export default (state = initialState, {type, payload}) => {
    switch (type){
        case SEARCH_SUCCESS:
            return payload.map(track => track)

        default:
            return state
    }
}